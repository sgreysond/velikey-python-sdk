#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="${PYTHON:-python3}"
CONFIG="${SCRIPT_DIR}/../scripts/velikey_hours_config.json"
"$PY" "${SCRIPT_DIR}/../scripts/validate_hours.py" commit-msg "$1" "$CONFIG"
